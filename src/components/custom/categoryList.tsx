import {Button} from "@/components/ui/button";

export const CategoryList = ({}) => {
    return (
        <div className="flex">
            <Button variant="outline">
                🏷️ Category name
            </Button>
        </div>
    );
};
